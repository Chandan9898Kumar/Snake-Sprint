import React, { memo } from "react";


const SnakeHeader = () => {
  return (
    <div className="flex">
      <div className="title">SNAKE</div>
    </div>
  );
};

export default memo(SnakeHeader);
