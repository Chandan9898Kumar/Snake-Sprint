import React, { memo } from "react";
// import "../../App.css";
// import "../../utils.css";
const GameBoard = () => {
  return (
    <div className="flex">
      <div className="game-board"></div>
    </div>
  );
};

export default memo(GameBoard);
